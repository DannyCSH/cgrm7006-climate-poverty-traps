# 第一轮修改说明

这份说明只记录第一轮已经真实完成并验证过的修改，不混入尚未完成的设想。

## 第一轮已经完成的内容

- 把基线模型从 2 个结果变量扩展到 5 个结果变量：
  - `poor215`
  - `poor365`
  - `poor685`
  - `gini`
  - `theil`
- 新增与 FE 完全对应的次国家 `long-difference` 结果
- 把原先混在一起的基线图拆成：
  - 贫困结果图
  - 不平等结果图
- 补出样本构造表，解释为什么不同表里的 `N` 会变化

## 目前已经验证的结果

### 1. 次国家 FE

- `poor215` 温度系数约为 `0.629`
- `poor365` 温度系数约为 `0.809`
- `poor685` 温度系数约为 `0.165`，不显著
- `gini` 温度系数约为 `0.463`
- `theil` 温度系数约为 `1.119`

解释上可以写成：

- 升温会显著提高较低贫困线下的贫困率
- 对最高贫困线 `poor685` 的影响在当前设定下不稳健
- 升温对不平等的正向影响在 `gini` 和 `theil` 上都能看到

### 2. 次国家 long-difference

- `poor215` 温度系数约为 `1.178`
- `poor365` 温度系数约为 `0.736`
- `poor685` 温度系数约为 `-0.125`，不显著
- `gini` 温度系数约为 `0.684`
- `theil` 温度系数约为 `1.273`

解释上可以写成：

- FE 与 LD 在方向上保持一致，说明主结果不是只靠短期波动撑起来的
- `poor685` 在 FE 和 LD 中都不显著，反而让故事更严谨，不会显得我们在强行夸大结果

## 样本构造的关键数字

- 原始面板观测值：`6727`
- 贫困变量可用样本：`6719`
- 不平等变量可用样本：`5736`
- FE 完整样本：
  - 贫困结果 `6052`
  - 不平等结果 `5143`
- 农业异质性完整样本：
  - 贫困结果 `6042`
  - 不平等结果 `5133`
- LD 完整样本：
  - 贫困结果 `1292`
  - 不平等结果 `1194`

## 这轮修改后建议的讲法

- 主结果：`次国家 FE + 次国家 LD`
- 主拓展：农业异质性
- 辅助对照：国家级聚合结果
- 附录探索：治理异质性

## 这轮先不要再写得太满的地方

- 不要把治理异质性写成主结论
- 不要把 risk score 直接写成“已经识别出 climate-poverty trap”
- 更稳妥的说法应该是：
  - 回归模型识别的是升温效应
  - 风险得分只是帮助识别更接近 trap risk 的地区

## 第一轮新增产出文件

- 基线 FE 结果：`outputs/10_baseline_fe_results.csv`
- 长差分结果：`outputs/15_long_difference_results.csv`
- 样本构造表：`tables/sample_construction.csv`
- 贫困 FE/LD 图：`figures/figure_1_poverty_fe_ld.png`
- 不平等 FE/LD 图：`figures/figure_2_inequality_fe_ld.png`

## 第二轮最应该接着做的内容

- 恢复温度分箱结果，把原文“高温区更显著”的核心结论补回来
- 更新中文 writeup / manual / poster 文稿
- 把第一轮新增图表和表格同步到 GitHub 仓库
